> Create a FastAPI project to manage vouchers through a CRUD API. A voucher should consist of a unique code (created by the system and non editable by users), a discount percentage, an expiration date, an active status, created/updated timestamps. The API should allow to create couchers, list vouichers (with pagination), retrireve vouchers by code if it has not expired and is still active, update vouchers, deactivate vouchers. It has to use SQLAlchemy, pydantic v2 and a mysql database. In addition to the codebase, provide a README to install dependencies on both UNIX-based and Windows systems as well as the sql file to create the database.

> Write a set of tests for it